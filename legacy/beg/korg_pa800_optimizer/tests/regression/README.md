# Regression Tests

The P0 gate list from `docs/VALIDATION_SPECIFICATION.md`, to run on
every change once the pipeline exists. Owned by Vertical C — see
`docs/VERTICAL_DECOMPOSITION.md`. Empty at this phase.
