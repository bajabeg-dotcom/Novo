# Ornament Detection Fixtures

Required per `docs/ORNAMENT_SPECIFICATION.md`: positive examples per
ornament category plus the same negative/false-positive categories as
`tests/fixtures/trills/`. Owned by Vertical B. Empty at this phase.
