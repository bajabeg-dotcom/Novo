# Migrations

Placeholder. Migration files will be added here once `schema.py` has an
initial version to migrate from. Other verticals proposing schema
changes add a file here (`vertical_b_*.sql` / `vertical_c_*.sql`)
rather than editing `schema.py` directly — see
`docs/VERTICAL_DECOMPOSITION.md`.
