"""Generative Terca/harmony voicing (GENERATIVE_MODE only, disabled by
default). Existing Terca tracks default to preservation mode -- see
master spec "Terca Engine": Solo -> chord -> scale -> candidates ->
voice leading -> range -> style DNA -> Gold DNA -> validation. Never a
naive pitch+3 transform. Not implemented yet.

Owning vertical: C.
"""
