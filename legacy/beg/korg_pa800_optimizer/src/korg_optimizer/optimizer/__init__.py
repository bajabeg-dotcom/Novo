"""Optimization engine: the single call surface used by both cli.py and
gui/main_window.py (docs/ARCHITECTURE.md "CLI/GUI shared-engine
contract"). Includes humanization, ornament/trill/harmony/delay
generation (generative modes, disabled by default), and the change log.

Owning vertical: C.
"""
