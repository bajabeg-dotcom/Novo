# Mappings

Reserved for any file-based mapping inputs (e.g. imported official
KORG documentation references used to seed `gm_rx_mapping` /
`rx_articulations`). Primary mapping storage is `knowledge.db` — see
`docs/DATABASE_ARCHITECTURE.md`.

Empty at this phase.
