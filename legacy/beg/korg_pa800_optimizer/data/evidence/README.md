# Evidence

Reserved for any non-database evidence artifacts (e.g. exported
evidence-inventory snapshots). Primary evidence storage is
`evidence.db` plus the raw files under `data/factory/{synthetic,real}/`
— see `docs/DATABASE_ARCHITECTURE.md`.

Empty at this phase.
